# Loaded automatically by `vite build` (production mode). This is the
# deployed FastAPI backend on Render — a public URL, not a secret.
# A hosting-panel env var of the same name still overrides this.
VITE_API_BASE_URL=https://vinayagamoorthy-jothidam-app.onrender.com
